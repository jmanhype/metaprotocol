# metaprotocol

Production-oriented Python coordination protocol for autonomous agents.

## Quickstart

```bash
pip install -e .
meta-protocol register --agent-id a --skill code_gen --complexity advanced --cost-per-use 0.15
meta-protocol discover --skills code_gen --format json
```
